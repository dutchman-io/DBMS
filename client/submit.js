//const submitBtn = document.querySelector("submit-form");

//submitBtn.onclick = function () {
    // const formData = document.querySelectorAll('#registrationForm input').value;
    // const name = nameInput.value;
    // nameInput.value ="";
    // console.log(formdata);

 const formData = document.querySelector('#surname').value;
 console.log(formData);
//  Array.from(document.querySelectorAll("#registrationForm input")).reduce((acc, input)=> ({...acc, [input.id]: input.value}),{});
// console.log(formData);

